1、简述版本更新的实现思路，在Android Studio中新建一个txt编写即可（20分）
    先把编译好的apk放到tomcat目录里然后再tomcat下的version.txt下记录一下版本号，然后启动tomcat，并且可以下载刚刚放在tomcat文件下的apk，
       在项目中访问刚刚创建好的version.txt文件对比里面的版本号，如果比记录的版本号高那就不用更新，如果当前版本号比记录的版本号第那就
       弹框提示用户进行版本更新。